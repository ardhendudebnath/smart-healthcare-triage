# Copy this file to ".env" in the same folder, then paste your real key after
# the "=" sign. The .env file is git-ignored, so your key never reaches GitHub.
#
# Get a free key at https://aistudio.google.com -> "Get API key"
#
# Leaving this blank is fine — the app falls back to offline spaCy extraction.
GEMINI_API_KEY=
